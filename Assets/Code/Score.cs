using UnityEngine;
using UnityEngine.UI;

public class Score : MonoBehaviour {

/*
    private Text text;

    void Start()

    {
        text = GetComponent<Text>();
    }
	
    void OnEnable()
    {
        GameManager.instance.updateScore += newScore;
    }

    void OnDisable()
    {
        GameManager.instance.updateScore -= newScore;
    }


    private void newScore()
    {
        text.text = "" + GameManager.instance.score;
    }
    */
}
